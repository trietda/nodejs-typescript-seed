// import { IntegrationEventManager } from '../infra';

export default class IntegrationEventRegister {
  static registerIntegrationEvent(): void {
    // const integrationEventManager = new IntegrationEventManager();
  }
}
